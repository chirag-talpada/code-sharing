import { createContext } from "react";

const codeContext=createContext();

// const codeProvider=({children})=>{
//     return <codeContext.Provider>{children}</codeContext.Provider>
// }

export default codeContext;